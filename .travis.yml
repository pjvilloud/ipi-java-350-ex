language : java

addons:
  sonarcloud:
    organization: "your-sonar-organization-name"
    token :
      secure : 64045e2694c7a52cebd5c3548aa30b1e677eb808 #encrypted-value of your token
script:
  # the following command line builds the project, runs the tests with coverage and then execute the SonarCloud analysis
  - mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent package sonar:sonar -Dsonar.projectKey=carole_ipi-java-350-ex